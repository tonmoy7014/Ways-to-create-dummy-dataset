# Ways-to-create-dummy-dataset
A dummy data set contains records that have the same content and layout as a real production data set, but all the data is fake. It is useful for testing different models
